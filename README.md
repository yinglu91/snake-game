1. clone project: git clone the repo
2. cd to the repo
3. seup virtual environment
   $ virtualenv venv
   $ source venv/Scripts/activate

4. $ pip install -r requirements.txt
5. run the program:
   Open the main.py, right click its content, Run Python File in Terminal
